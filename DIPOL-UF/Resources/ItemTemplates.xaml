<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DIPOL_UF.Resources"
                    xmlns:vm="clr-namespace:DIPOL_UF.ViewModels"
                    xmlns:cmnds="clr-namespace:DIPOL_UF.Commands"
                    xmlns:cnvrt="clr-namespace:DIPOL_UF.Converters">
    <HierarchicalDataTemplate x:Key="MenuBarItemTemplate" DataType="{x:Type vm:MenuItemViewModel}"
                              ItemsSource="{Binding MenuItems}">
        <Label Content="{Binding Header}"/>
    </HierarchicalDataTemplate>

    <HierarchicalDataTemplate x:Key="CameraTreeViewItemTemplate" DataType="{x:Type vm:ConnectedCamerasTreeViewModel}"
                              ItemsSource="{Binding CameraList}">
        <ContentPresenter Content="{Binding Name}"/>
        <HierarchicalDataTemplate.ItemTemplate>
            <DataTemplate>
                <ContentPresenter Content="{Binding Value}"/>
            </DataTemplate>
        </HierarchicalDataTemplate.ItemTemplate>
     </HierarchicalDataTemplate>

    <DataTemplate x:Key="FoundCamerasListViewItemTemplate">
        <!--<DockPanel  Background="Transparent">
            <Label DockPanel.Dock="Left" HorizontalAlignment="Left" VerticalAlignment="Center" Content="{Binding Value}"/>
        </DockPanel>-->
        <!--<ContentPresenter Content="{Binding Key, Converter={StaticResource ResourceKey=CameraKeyToStringConverter}}"/>-->
        <StackPanel Orientation="Horizontal">
            <Label Width="125" Content="{Binding Key, Converter={StaticResource ResourceKey=CameraKeyToStringConverter}}"/>
            <Label Content="{Binding Value}"/>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>