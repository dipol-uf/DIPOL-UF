<UserControl x:Class="DIPOL_UF.Controls.DipolImagePresenter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:cmnds="clr-namespace:DIPOL_UF.Commands"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Background="Beige">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image 
               HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
               Source="{Binding BitmapSource, Mode=OneWay}" Margin="10" MouseMove="{cmnds:EventToCommand MouseHoverCommand}"/>
        <Border Grid.Row="0" Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                MinWidth="200" MinHeight="200" BorderThickness="2" BorderBrush="Aqua" >
            <Path Data="{Binding SamplerPath, Converter={StaticResource ResourceKey=GeometryRulesToStreamGeometryConverter}}" 
                  HorizontalAlignment="Left" VerticalAlignment="Top"
                  StrokeThickness="2" Stroke="Blue" RenderTransformOrigin="0, 0">
                <Path.RenderTransform>
                    <TranslateTransform X="{Binding SelectionX}" Y ="{Binding SelectionY}"/>
                </Path.RenderTransform>
            </Path>
            <!--<Image Source="{Binding TestImage}"
                HorizontalAlignment="Center" VerticalAlignment="Center" 
                Margin="10">
                --><!--<Image.OpacityMask>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="Black" Offset="0"/>
                        <GradientStop Color="White" Offset="1"/>
                    </LinearGradientBrush>
                </Image.OpacityMask>--><!--
            </Image>-->
        </Border>
        
        <Grid Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Slider Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                    HorizontalAlignment="Stretch" MaxWidth="300"
                    Minimum="{Binding ImgScaleMin}"
                    Maximum="{Binding ImgScaleMax}"
                    Value="{Binding ThumbLeft, UpdateSourceTrigger=PropertyChanged}"
                    ValueChanged="{cmnds:EventToCommand ThumbValueChangedCommand}"
                    />
            <Slider Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                    HorizontalAlignment="Stretch" MaxWidth="300"
                    Minimum="{Binding ImgScaleMin}"
                    Maximum="{Binding ImgScaleMax}"
                    Value="{Binding ThumbRight, UpdateSourceTrigger=PropertyChanged}"
                    ValueChanged="{cmnds:EventToCommand ThumbValueChangedCommand}"/>

        </Grid>
    </Grid>
</UserControl>
